//import React from 'react'
//import dayjs from 'dayjs'
import DayjsWrapper from '../utils/DayjsWrapper'

export var ClndrData = {
    year: DayjsWrapper.getCurrentDateYearValue(),
    month: DayjsWrapper.getCurrentDateMonthValue(),
  //  day: DayjsWrapper.getCurrentDayDateValue(),
}

//export const ClndrContext = React.createContext(ClndrData)
